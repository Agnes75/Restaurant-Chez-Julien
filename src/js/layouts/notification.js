const notification = function () {
  
  return `
            <aside class="notification" id="notification">
             </aside>
        
        `;
};

export default notification;
// ici on ecrit une fonction qui aura pour objet de construire
// une balise aside sur toutes les pages, et qui sera remplie ou non 
//par un message à définir variant selon les situations